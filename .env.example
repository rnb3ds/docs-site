# No required env for the docs site itself.
#
# Search is fully local: scripts/generate-search-index.ts builds per-project
# + per-language JSON indexes into docs/public/search-index/, and the
# ProjectSearch component fetches them at runtime. No credentials needed.
