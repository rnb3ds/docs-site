---
title: "Справочник API — HTTPC"
description: "Индекс справочника API HTTPC: навигация по трём группам — основные, запросы/ответы и продвинутые функции — со ссылками на функции уровня пакета, 27 функций параметров запросов WithXxx, систему конфигурации Config, 8 встроенных фабрик промежуточного ПО, доменный клиент, управление сессиями, загрузку файлов и типы ошибок."
---

# Справочник API

HTTPC предоставляет 27 функций параметров запросов, 5 предустановок конфигурации, 8 встроенных промежуточных ПО и полную поддержку загрузки файлов.

## Основная архитектура

```text
пакет httpc
├── интерфейс Client — основной клиент, поддерживает все HTTP-методы
├── интерфейс DomainClienter — клиент области домена со встроенным управлением сессиями
├── Config — система конфигурации (таймауты/соединения/безопасность/повторы/промежуточное ПО)
├── RequestOption — 27 функций параметров запросов
├── MiddlewareFunc — цепочка промежуточного ПО
├── Result — результат ответа (включая метаданные запроса)
└── Функции уровня пакета — использование без создания клиента
```

## Навигация по модулям

### Основные

| Модуль | Описание |
|--------|----------|
| [Функции пакета](./functions) | Функции уровня пакета Get/Post/Put/Patch/Delete, методы клиента и вспомогательные функции |
| [Конфигурация](./config) | Структура Config, 5 предустановок, функция валидации и безопасность Cookie |
| [Интерфейсы](./interfaces) | Основные интерфейсы Client, Doer, DomainClienter, RetryPolicy и др. |
| [Result](./result) | Типы Result, RequestInfo, ResponseInfo, RequestMeta и все методы |

### Запросы и ответы

| Модуль | Описание |
|--------|----------|
| [Параметры запросов](./options) | 27 функций параметров WithXxx (заголовки, тело, аутентификация, Cookie, обратные вызовы) |
| [Промежуточное ПО](./middleware) | Композиция Chain, 8 встроенных фабрик и тип события аудита |
| [Типы ошибок](./errors) | ClientError, 12 значений перечисления ErrorType и 13 переменных ошибок |

### Продвинутые функции

| Модуль | Описание |
|--------|----------|
| [Доменный клиент](./domain-client) | Создание DomainClient, HTTP-методы, методы загрузки и правила сборки URL |
| [Управление сессиями](./session) | Управление Cookie/заголовками SessionManager и проверка безопасности |
| [Загрузка файлов](./download) | Функции загрузки, DownloadConfig, возобновление загрузки и защита безопасности |
| [Константы и типы](./constants) | Перечисление BodyKind, FormData/FileData и ключи контекста аудита |

## Быстрый справочник

### Создание клиента

```go
client, err := httpc.New()                    // конфигурация по умолчанию
client, err := httpc.New(httpc.SecureConfig()) // безопасная предустановка
client, err := httpc.New(customConfig)         // пользовательская конфигурация
```

### Отправка запросов

```go
// Функции уровня пакета
result, err := httpc.Get(url, options...)

// Методы клиента
result, err := client.Get(url, options...)

// С контекстом
result, err := client.Request(ctx, "GET", url, options...)
```

### Обработка ответов

```go
result.StatusCode()           // код состояния
result.Body()                 // тело ответа (строка)
result.RawBody()              // тело ответа (байты)
result.Unmarshal(&data)       // парсинг JSON
result.IsSuccess()            // является ли 2xx
result.Meta.Duration          // время выполнения запроса
result.Meta.Attempts          // количество повторных попыток
defer httpc.ReleaseResult(result) // возврат в пул объектов
```
