---
title: Оптимизация производительности - HTTPC
description: Руководство по оптимизации производительности HTTPC — сравнение четырёх предустановленных конфигураций, настройка пула соединений, стратегия повторного использования пула объектов Result и решения распространённых антипаттернов.
---

# Оптимизация производительности

## Сравнение предустановленных конфигураций

| Показатель | Default | Secure | Performance | Minimal |
|------|---------|--------|-------------|---------|
| Таймаут Request | 180s | 15s | 60s | 180s |
| MaxIdleConns | 50 | 20 | 100 | 10 |
| MaxConnsPerHost | 10 | 5 | 20 | 2 |
| MaxRetries | 3 | 1 | 3 | 0 |
| MaxResponseBodySize | 10MB | 5MB | 50MB | 1MB |
| HTTP/2 | Включён | Включён | Включён | Включён |
| Cookies | Выключены | Выключены | Включены | Выключены |
| Защита от SSRF | Включена | Включена | Включена | Включена |
| FollowRedirects | Включены | Выключены | Включены | Выключены |

## Выбор по сценарию

| Сценарий | Рекомендуемая предустановка | Рекомендации по настройке |
|------|----------|----------|
| Универсальный веб-сервис | Default | - |
| Обработка URL от пользователей | Secure | - |
| Внутренние микросервисы с высокой нагрузкой | Performance | Увеличить MaxIdleConns |
| Одноразовые скрипты | Minimal | - |
| Сервис загрузки файлов | Performance | Увеличить MaxResponseBodySize |
| Финансовые/медицинские API | Secure + пользовательская | Добавить аудитный middleware |

```go
// Сценарий с высокой пропускной способностью
client, _ := httpc.New(httpc.PerformanceConfig())

// Тонкая настройка на основе предустановки
cfg := httpc.PerformanceConfig()
cfg.Timeouts.Request = 120 * time.Second
cfg.Connection.MaxIdleConns = 200
client, _ := httpc.New(cfg)
```

## Повторное использование пула объектов

HTTPC имеет встроенный пул объектов Result. Используйте `ReleaseResult` для возврата:

```go
result, err := client.Get(url)
if err != nil {
    return err
}
defer httpc.ReleaseResult(result) // Возврат в пул объектов
```

:::tip Совет
В сценариях с высокой параллельной нагрузкой `ReleaseResult` значительно снижает нагрузку на GC.
:::

## Антипаттерны производительности

| Антипаттерн | Причина | Правильный подход |
|--------|------|----------|
| Создание клиента для каждого запроса | Соединения не переиспользуются | Глобальное использование клиента |
| Игнорирование ReleaseResult | Увеличение нагрузки на GC | Возврат через defer |
| Слишком большой MaxResponseBodySize | Высокое потребление памяти | Устанавливать разумные лимиты |
| Использование result.String() на горячем пути | Накладные расходы на создание строки | Использовать Body() напрямую |

## Далее

- [Пул соединений и прокси](./connection-pool) — выбор параметров пула соединений, настройка прокси и DoH
- [Обработка ошибок](./error-handling) — многоуровневая стратегия таймаутов
- [Обзор безопасности](../security/) — баланс безопасности и производительности
