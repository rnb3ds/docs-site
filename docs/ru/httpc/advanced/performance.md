---
title: "Оптимизация производительности — HTTPC"
description: "Руководство по оптимизации производительности HTTPC: сравнение четырёх предустановок Default/Secure/Performance/Minimal и выбор по сценарию, тонкая настройка пула соединений и таймаутов на основе предустановок, повторное использование пула объектов ReleaseResult для снижения нагрузки на GC и распространённые антипаттерны производительности."
---

# Оптимизация производительности

## Сравнение предустановок конфигурации

| Показатель | Default | Secure | Performance | Minimal |
|-------------|---------|--------|-------------|---------|
| Request таймаут | 180s | 15s | 60s | 180s |
| MaxIdleConns | 50 | 20 | 100 | 10 |
| MaxConnsPerHost | 10 | 5 | 20 | 2 |
| MaxRetries | 3 | 1 | 3 | 0 |
| MaxResponseBodySize | 10MB | 5MB | 50MB | 1MB |
| HTTP/2 | Включён | Включён | Включён | Включён |
| Cookies | Выключены | Выключены | Включены | Выключены |
| Защита SSRF | Включена | Включена | Включена | Включена |
| FollowRedirects | Включено | Выключено | Включено | Выключено |

## Выбор по сценарию

| Сценарий | Рекомендуемая предустановка | Рекомендации по настройке |
|----------|-----------------------------|--------------------------|
| Общие веб-сервисы | Default | - |
| Обработка пользовательских URL | Secure | - |
| Внутренние микросервисы с высокой нагрузкой | Performance | Увеличить MaxIdleConns |
| Одноразовые скрипты | Minimal | - |
| Сервис загрузки файлов | Performance | Увеличить MaxResponseBodySize |
| Финансовые/медицинские API | Secure + пользовательская | Добавить промежуточное ПО аудита |

```go
// Сценарий высокой пропускной способности
client, _ := httpc.New(httpc.PerformanceConfig())

// Тонкая настройка на основе предустановки
cfg := httpc.PerformanceConfig()
cfg.Timeouts.Request = 120 * time.Second
cfg.Connection.MaxIdleConns = 200
client, _ := httpc.New(cfg)
```

## Повторное использование пула объектов

HTTPC имеет встроенный пул объектов Result, используйте `ReleaseResult` для возврата:

```go
result, err := client.Get(url)
if err != nil {
    return err
}
defer httpc.ReleaseResult(result) // Возврат в пул объектов
```

:::tip Совет
В высоконагруженных сценариях `ReleaseResult` значительно снижает нагрузку на GC.
:::

## Антипаттерны производительности

| Антипаттерн | Причина | Правильный подход |
|-------------|---------|-------------------|
| Создание клиента на каждый запрос | Соединения не переиспользуются | Глобально переиспользуйте клиент |
| Игнорирование ReleaseResult | Увеличение нагрузки на GC | Возврат через defer |
| Слишком большой MaxResponseBodySize | Высокое потребление памяти | Установите разумный лимит |
| Использование result.String() на горячем пути | Накладные расходы на построение строки | Используйте Body() напрямую |

## Что дальше

- [Пул соединений и прокси](./connection-pool) — выбор параметров пула соединений, настройка прокси и DoH
- [Обработка ошибок](./error-handling) — стратегия разделения таймаутов
- [Обзор безопасности](../security/) — баланс безопасности и производительности
