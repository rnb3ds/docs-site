---
title: "Справочник API - HTML"
description: "Полный указатель справочной документации по API библиотеки CyberGo HTML, включая два режима вызова — функции пакета (удобный режим) и Processor (режим экземпляра), охватывающий извлечение контента, извлечение текста, форматированный вывод, извлечение ссылок, пакетную обработку, управление конфигурацией, журнал аудита и определения типов с подробными описаниями и навигацией по модулям."
---

# Справочник API

Библиотека HTML предоставляет следующие ключевые компоненты:

| Компонент | Описание | Документация |
|------|------|------|
| Функции пакета | Удобные функции для однократных вызовов | [Функции пакета](./functions) |
| Processor | Экземпляр процессора с повторным использованием ресурсов и кэша | [Processor](./processor) |
| Config | Структура конфигурации и предустановки | [Конфигурация](./config) |
| Форматы вывода | Вывод в Markdown, JSON | [Форматы вывода](./output) |
| Извлечение ссылок | Независимый API извлечения ссылок | [Извлечение ссылок](./links) |
| Пакетная обработка | Параллельное пакетное извлечение | [Пакетная обработка](./batch) |
| Интерфейсы | Extractor, StatsProvider и др. | [Определение интерфейсов](./interfaces) |
| Типы | Result, ImageInfo и др. | [Определения типов](./types) |
| Константы и ошибки | Значения по умолчанию, сигнатурные ошибки | [Константы и ошибки](./constants) |
| Система аудита | Конвейер аудита и Sink | [Система аудита](./audit) |

## Обзор API

### Два режима вызова

```text
┌─────────────────────────────────────────┐
│      Функции пакета (удобный режим)       │
│  html.Extract(data) → *Result, error    │
│  Внутренне используют sync.Pool          │
│  для повторного использования Processor  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│       Processor (режим экземпляра)        │
│  p, _ := html.New(cfg)                  │
│  defer p.Close()                        │
│  result, err := p.Extract(data)         │
│  ✓ Повторное использование кэша          │
│  ✓ Сбор статистики  ✓ Журнал аудита      │
└─────────────────────────────────────────┘
```

### Правила именования функций

| Паттерн | Именование | Пример |
|------|------|------|
| Базовый | `Extract*` | `Extract`, `ExtractText` |
| Из файла | `Extract*FromFile` | `ExtractFromFile` |
| С контекстом | `Extract*WithContext` | `ExtractWithContext` |
| Из файла + контекст | `Extract*FromFileWithContext` | `ExtractFromFileWithContext` |

### Информация о модуле

- **Путь к модулю**: `github.com/cybergodev/html`
- **Версия Go**: 1.25+
- **Зависимости**: `golang.org/x/net`, `golang.org/x/text`
